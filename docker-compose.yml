version: '3.9'

services:

  fernanda-ai:

    build: .

    container_name: fernanda-ai

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped

